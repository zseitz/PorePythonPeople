[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"

[project]
name = "NanoporeAnalysis"
version = "0.0.0"
description ="nanopore analysis pipeline"
dependencies = [
    "numpy",
    "pytest",
]
