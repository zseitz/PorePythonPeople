[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"

[project]
name = "entropy"
version = "0.0.0"
description ="entropy testing demo"
dependencies = [
    "numpy",
    "pytest",
]
